/** 
 *function criarProduto(nome, preco) {
 *  const produto = {nome, preco, desconto: preco*0.1}
 *  return produto
 *}
 */

const criarProduto = (nome, preco) => produto = {nome, preco, desconto: preco*0.05}

console.log(criarProduto("Celular", 2000))